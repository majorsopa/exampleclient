plugins {
    id 'java'
}

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

jar {
    manifest {
        attributes(
                "Agent-Class": "org.example.Main",
//                "Can-Redefine-Classes": true,     will be needed if you want to use hooks
//                "Can-Retransform-Classes": true,  see above
        )
    }
}